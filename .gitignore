node_modules/
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*

# Build artifacts
# NOTE: Both bundles are COMMITTED to repo because x402 packages
# are not published to npm and Vercel can't build them. We commit the bundles.
# public/client-signer.bundle.js.map
# lib/x402/coinbase.bundle.js.map (if sourcemaps are generated)

# Local development files
FIX-LINKS.sh
INSTALL-X402.md
SETUP-LINK.md
SETUP.md
build.md
